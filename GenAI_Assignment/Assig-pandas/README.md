How to run
Open Pandas_Assignment.ipynb in Jupyter Notebook, JupyterLab, or VS Code.
Run all cells top to bottom (Kernel > Restart & Run All).
Requires pandas (and its plotting backend, matplotlib, installed as a dependency) — no other packages needed.
